node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
.vercel
.env.local
.env*.local

# Gerado por `npx convex dev` a partir de schema.ts e das funções.
# Fora do controle de versão de propósito: o conteúdo depende do deployment
# configurado na máquina, e um arquivo desatualizado aqui causa erro de tipo
# que parece bug de código.
convex/_generated/
